{
  "name": "repasoexamen2",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server:up": "json-server --watch src/data/db.json --port 1492"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3",
    "vite": "^7.2.4"
  }
}
